/** Navigation link for the top nav. */
export type NavItem = {
  id: string;
  label: string;
  href: string;
};

/** Brand mark for the navbar. */
export type Brand = {
  monogram: string;
  label: string;
};
